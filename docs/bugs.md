Ventana editor manual:
- El botón "CALCULAR" de la ventana del editor no inicia desactivada (al menos, con seguridad, no visualmente). Al desactivar y activar el checkbox de calculo automático recobra su comportamiento deseado (al menos visualmente).
- Al hacer clic al botón "CALCULAR" activado (por desactivación del calculo automático) realiza los calculos. Sin embargo, visualmente no pareciera que se estuviera presionando. 
- En primer arranque de la ventana del editor manual abre maximizada. Esto es correcto y deseado, ya que no hay memoria de haber sido reducida ni movida anteriormente. Pero al reducirla, bajo esas condiciones (es decir, sin referencia anterior), debería reducirse a la mitad de altura, la mitad de ancho y ser ubicada en la esquina superior derecha del monitor actual. Así efectivamente sucedía anteriormente y ya no lo hace.

¿Duplicaciones de la constante MAX_TEXT_CHARS (dependiente de constants.js) en distintos archivos?

Quitar sonido a todos los avisos